export type Employee = {
    id: number;
    name: string;
    dni: string;
    nss: string;
}

export type Company = {
    id: number;
    name: string;
    cif: string;
    direccion: string;
    cp: string;
    ciudad: string;
}